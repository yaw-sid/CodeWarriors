import { Validator } from "validators";

export class TypographicalValidator implements Validator {
    validate(dom: any, element: Element): boolean {
        throw new Error("Method not implemented.");
    }
}